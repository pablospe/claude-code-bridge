# Milestone 3: Observational hook-relayed visibility

> Status: Locked plan, ready to execute.

## Goal

After M3, a consumer reading `events(sessionId)` sees a tool timeline — the Bash
commands, Edit / Write / Read calls, Grep / Glob searches — interleaved with the
message-level events M1 already delivers. The render goes from "what claude
finally said" to "what claude did, in order, with what inputs and results."

Concrete delta for a prompt like "fix the bug in foo.ts":

Before M3 (M1 + M2 surface):

```
[session.started]  sid=…
[agent.reply]      "I'll take a look at foo.ts."
[agent.reply]      "Found and fixed the off-by-one on line 42."
[agent.done]       final=true
[session.ended]    reason="agent done"
```

After M3 (hook relay on, three minimum events registered):

```
[session.started]  sid=…
[agent.reply]      "I'll take a look at foo.ts."
[tool.event]       PreToolUse  Read   {file_path:"…/foo.ts"}
[tool.event]       PostToolUse Read   (1.2 KB, truncated:false)
[tool.event]       PreToolUse  Edit   {file_path:"…/foo.ts", old_string:"…"}
[tool.event]       PostToolUse Edit   ok
[agent.reply]      "Found and fixed the off-by-one on line 42."
[tool.event]       Stop        (per-message)
[agent.done]       final=true
[session.ended]    reason="agent done"
```

The `tool.event` BridgeEvent variant was reserved during M1 for exactly this
purpose and is currently dead code. M3 makes it live.

**Ordering.** `tool.event` records land in **arrival order** as the bridge
receives them. Consumers reconstruct pre/post pairs by matching on
`payload.data.tool_use_id`. `Stop` is per-agent-message — the authoritative
end-of-turn marker remains `agent.done`.

## Non-goals

- **Token-level streaming.** Indefinitely deferred. Every available path
  (Agent SDK; `--print --output-format stream-json`) re-violates a stated M1
  non-goal. Channels deliver complete messages, not tokens.
- **Extended thinking blocks.** Indefinitely deferred for the same reason — no
  path that respects M1's non-goals.
- **Permission-prompt routing.** Deferred to M4 (consumer-demand-gated).
- **Hook-driven behavior modification.** `PreToolUse` can block tools or
  rewrite tool inputs; M3 stays strictly observational. The relay never writes
  a `permissionDecision` back.
- **Phase-2 hook events.** See "Deferred to phase 2" below.

## Design (locked)

### `BridgeEvent` shape: catch-all `tool.event`

```ts
{ type: "tool.event"; sessionId: string;
  payload: { event: string; data: unknown } }
```

Honors the variant M1 already reserved. `payload.event` is the hook event name
verbatim (`"PreToolUse"`, `"PostToolUse"`, `"Stop"`). `payload.data` is the
hook payload, after the size policy below has been applied. Consumers narrow
on `payload.event` at their convenience.

Rationale: hooks are an evolving surface owned upstream. Keeping the public
`BridgeEvent` union stable insulates consumers from churn in claude's hook set
and from any future renames. Promoting specific variants is a non-breaking
follow-up that can happen if a concrete consumer reports the introspection
cost.

### Control-protocol `hook` envelope (wire shape)

The relay bin and the `ControlServer` speak this frame over the existing
JSON-lines transport. It is independent of claude's evolving hook payload
shape: claude's raw payload is carried verbatim inside `payload`.

```ts
{ kind: "hook";
  sessionId: string;          // value of CCB_SESSION_ID in the relay's env
  event: "PreToolUse" | "PostToolUse" | "Stop";
  payload: Record<string, unknown>;  // claude's raw hook payload, post-truncation
  sentAt: string;             // ISO-8601 timestamp at relay send-time
}
```

Bridge fan-in: each received frame becomes one `BridgeEvent.tool.event`
record with `payload = { event: frame.event, data: frame.payload }`.
`sentAt` is dropped from the public event (consumers get the bridge's own
`recordedAt`) but is used by tests to assert ordering.

### Identity and session correlation

The relay bin tags every frame with the value of its `CCB_SESSION_ID` env var,
the same variable the channel server already reads. The bridge filters and
routes by that tag.

The bridge does NOT inspect any `session_id` field that may appear inside the
hook payload itself. The hook's internal `session_id` is embedded in
`payload.data` verbatim for consumer use, but plays no role in bridge routing.
This sidesteps the question of whether claude's internal session id equals
`CCB_SESSION_ID`: M3 makes that question irrelevant.

### Minimum hook set (phase 1)

Exactly three events: `PreToolUse`, `PostToolUse`, `Stop`. Smallest set
sufficient to render a tool timeline plus an end-of-turn marker. See
"Deferred to phase 2" for the rest.

### Payload size policy

Hard cap of 64 KB (65 536 bytes, measured **per field, independently**, on the
JSON-serialized field value) for `tool_input` and `tool_result`. When one or
more fields are truncated, set `truncated_fields: string[]` on the same object
that holds them, listing the field names that were cut. The 64 KB constant is
not configurable in phase 1; it is documented in the spec.

Truncation rule details:

- Measurement target: `Buffer.byteLength(JSON.stringify(value), "utf8")` of the
  field value (the *contents* of `tool_input` or `tool_result`, not the field
  name + value).
- If the value is a string ≤ 64 KB: pass through whole.
- If the value is a string > 64 KB: keep the first 65 536 bytes (UTF-8 safe
  boundary — round down to the last full code point), add the field name to
  `truncated_fields`.
- If the value is an object/array: re-emit only if its serialized size ≤ 64 KB.
  Otherwise, replace with the string `"<truncated: object exceeded 65536 bytes>"`
  and add the field name to `truncated_fields`. No selective per-property
  truncation in phase 1.
- `truncated_fields` is omitted (not `[]`) when nothing was truncated.

Metadata fields (`tool_name`, `tool_use_id`, timestamps, anything that isn't
`tool_input` or `tool_result`) pass through whole.

Example, `tool_input` truncated:

```json
{
  "event": "PreToolUse",
  "data": {
    "tool_name": "Bash",
    "tool_use_id": "toolu_01…",
    "tool_input": { "command": "<first 64 KB of the command>" },
    "truncated_fields": ["tool_input"]
  }
}
```

The `truncated_fields` key is CCB-added metadata living inside `payload.data`.
Consumers that round-trip the hook payload back to claude's hook schema MUST
strip this key.

### Readiness gate (shared with M2 — supervisor side shipped)

The "bridge is reachable" contract has two surfaces.

1. **`ClaudeCodeSupervisor.start()`** resolves only after the channel server's
   first `hello` for this session arrives at the bridge. Bounded by
   `Bridge.startTimeoutMs` at the layer above. **Shipped during M2** as
   commit `178b995` (supervisor side) plus `2443b47`
   (`ControlServer.deliver` waits for hello up to `deliverWaitMs`, default
   30s). M3 inherits this — no work needed.

2. **Hook relay bin (each fire)** runs its own short hello handshake before
   sending the `hook` frame. If the bridge is unreachable for any reason, the
   bin logs to stderr and exits 0. The bin MUST NOT fail claude's hook: a
   failing hook can block claude's turn, and observational visibility is not
   worth that risk. M3 owns this part.

   **Relay time budget (hardcoded, non-configurable in phase 1):**
   - Connect: 200 ms
   - Hello round-trip: 200 ms
   - Send frame + close: 100 ms
   - Total wall-clock cap: **500 ms** from process start to exit.

   On any timeout, transport error, JSON-parse failure on stdin, or missing
   `CCB_SESSION_ID`: write one line to stderr prefixed `ccb-hook-relay:` and
   exit 0. The bin never throws, never writes to stdout, and never blocks on
   anything that could outlive the budget.

The bridge tolerates hook frames received before the channel server's first
hello completes via a bounded in-memory queue: cap 100 frames per session,
oldest dropped on overflow, flushed after the channel-server hello arrives.
Hook frames received AFTER the channel server's peer-close are dropped
silently — the session is shutting down and there is no live consumer to fan
out to. Overflow drops are counted per session and exposed via a
`bridge.metrics().preHelloHookDrops` accessor (test/debug only — not a
`BridgeEvent`). Duplicate `hello` from the channel server is a no-op (already
hello'd).

## Scope

1. **Relay bin.** `packages/mcp-channel/src/hook-relay.ts` plus a new bin
   entry `ccb-hook-relay`, mirroring `ccb-channel-server`'s structure. Reads
   the hook payload JSON from stdin, runs the hello handshake, sends one
   `hook` frame, exits 0.
2. **Control protocol extension.** Add the `hook` envelope variant to
   `ControlMessageSchema` in `packages/mcp-channel/src/control.ts`. Add a new
   `hook` event on `ControlServer`'s emitter; supervisor adapters listen and
   fan it into the bridge as `tool.event` records.
3. **Bounded pre-hello queue.** Per-session, cap 100 frames, oldest-dropped,
   flushed on hello, drained / discarded on peer-close.
4. ~~`supervisor.start` hello gate.~~ **Already shipped in M2** (`178b995`).
   No M3 work; see "Readiness gate" above.
5. **`ccb hooks-config` CLI subcommand.** Emits the settings.json snippet
   that registers the relay for the three minimum events. Accepts `--out
   <path>` to write the snippet; default is stdout. Phase 1 does not auto-
   merge into existing settings.json files — that decision is left as an
   open question (see below).
6. **Managed-launch integration.** `ClaudeCodeSupervisor` accepts a
   `hooks?: { events: Array<"PreToolUse" | "PostToolUse" | "Stop"> }` option.
   When set, the supervisor writes a temp settings.json registering the
   relay for the requested events and passes its path via `--settings`. Off
   by default; turning it on is one option flag. **Temp settings lifecycle:**
   the file is created with mode `0600` under `os.tmpdir()` and `unlink`ed in
   the supervisor's `close()` handler (mirrors `ccb-launcher`'s pattern). Any
   hook fired AFTER close is already in the "dropped silently after peer-close"
   bucket per the readiness-gate spec, so the unlink race is tolerated.
7. **`formatPretty` rendering.** `apps/ccb`'s pretty renderer handles
   `tool.event` records on one line, e.g.
   `[tool.event] PreToolUse Bash "git status"`. Truncation is rendered as
   `(truncated)` next to the affected field.
8. **Docs.** SMOKE.md gets a hook-relay section. README adds a one-paragraph
   pointer.

## Task graph

Each task follows the M1/M2 discipline: TDD red-green within the task, commit
on green, then `/pr-review-multi --models claude,gemini,codex` against the
local diff before moving on.

### M3.1 — Control protocol `hook` envelope + bridge fan-in

Files: `packages/mcp-channel/src/control.ts`, `packages/core/src/bridge.ts`,
matching tests. `packages/core/src/events.ts` is untouched (variant exists).

Add `HookMessageSchema` to `ControlMessageSchema`'s discriminated union;
route incoming `hook` frames to a new `hook` event on `ControlServer`; in
the bridge, translate `hook` frames to `BridgeEvent.tool.event` records,
applying the 64 KB payload-size policy. Unit tests cover schema
accept/reject, fan-in to the bus, truncation marker placement.

Depends on: nothing.

### M3.2 — Bounded pre-hello queue (depends on M3.1)

Files: bridge / supervisor adapter glue; matching test.

TDD a per-session queue cap of 100 frames, oldest-dropped, flushed after
channel-server hello, discarded silently after peer-close. Tests cover:
overflow drop counter increments correctly, duplicate hello is idempotent,
hook frame arriving before session object exists is queued (or dropped
deterministically — pick during implementation and document), peer-close
during flush stops the flush cleanly.

### M3.3 — Hook relay bin (depends on M3.1; parallel with M3.2)

Files: `packages/mcp-channel/src/hook-relay.ts`,
`packages/mcp-channel/package.json` bin entry, matching test.

TDD against a fake control server. Cover: happy path; bridge-unreachable
(stderr + exit 0); malformed stdin (stderr + exit 0); connect timeout >200ms
(stderr + exit 0); hello timeout >200ms (stderr + exit 0); missing
`CCB_SESSION_ID` (stderr + exit 0, no connect attempt). Total wall-clock
budget 500 ms enforced via a top-level `Promise.race` against
`setTimeout(500)`. Stderr prefix `ccb-hook-relay:`. Use the existing
JSON-lines transport plus hello handshake. Tag frames with `CCB_SESSION_ID`;
never read `session_id` from payload for routing.

### M3.4 — `supervisor.start` hello gate ✅ SHIPPED in M2

This task is already complete. The hello gate landed during M2 work as
commit `178b995` ("feat(claude-code): supervisor.start awaits channel
server hello") and is in `packages/claude-code/src/claude-supervisor.ts`'s
`#waitForChannelHello` private method. Fake-launcher tests were updated
to spin up a real `ControlClient` whose `connect()` fires the synthetic
hello. `Bridge.startTimeoutMs` bounds the wait at the layer above.

A complementary `ControlServer.deliver` hello-gate also landed in `2443b47`
("feat(mcp-channel): ControlServer.deliver waits for hello arrival up to
deliverWaitMs"). Together they remove the "no connected client" race that
this milestone's prerequisite section called out.

**No work needed in M3.** M3.5 now depends only on M3.3.

### M3.5 — Managed-launch `hooks` option + `ccb hooks-config` (depends on M3.3)

Files: `packages/claude-code/src/claude-supervisor.ts`,
`packages/claude-code/src/config.ts`, `apps/ccb/src/cli.ts`, matching tests.

Extend `ClaudeCodeSupervisor` with the `hooks` option; generate a temp
settings.json; pass via `--settings`; clean up on close. Implement
`ccb hooks-config` emitting the snippet to stdout or `--out`. Unit-test the
snippet shape; integration-test the supervisor option with the fake launcher.

### M3.6 — `formatPretty` for `tool.event` (depends on M3.1)

Files: `apps/ccb/src/format.ts` or equivalent (+ test).

Render one line per `tool.event`: event name, tool name, short input
summary, truncation marker. Snapshot tests for the three minimum event
shapes.

### M3.7 — SMOKE + README + gated integration test (depends on M3.5 and M3.6)

Files: `docs/SMOKE.md`, `README.md`, `scripts/smoke-scripted.ts` (or sibling).

Document the hook-relay path in SMOKE.md. Add a `CCB_RUN_REAL_CLAUDE=1`-gated
integration test (same gate as M2) using a **deterministic prompt** that
forces a specific tool — e.g., `"Read README.md and report the first line"`,
which deterministically triggers a `Read` tool call. Assertions:

1. At least one `tool.event` for each of `PreToolUse`, `PostToolUse`, `Stop`.
2. **Ordering**: every `PostToolUse` is preceded by a `PreToolUse` with the
   same `payload.data.tool_use_id`. Frames out of order = test failure.
3. `Stop` arrives at least once before `agent.done`.

**Parallelism plan.** M3.1 blocks everything. After M3.1: M3.2 / M3.3 / M3.6
are independent. M3.5 depends on M3.3. M3.7 joins M3.5 and M3.6.
(M3.4 is already shipped in M2's history; see above.)

## Success criteria

- Unit tests cover the new `hook` envelope, the bridge fan-in, per-field
  64 KB truncation with `truncated_fields`, the bounded pre-hello queue
  (overflow counter + duplicate-hello idempotency + peer-close drain), and
  the relay bin's six failure modes (bridge-unreachable, malformed stdin,
  connect timeout, hello timeout, missing `CCB_SESSION_ID`, malformed-shape
  payload).
- `bun run lint` and `bun run typecheck` stay green.
- `formatPretty` renders `tool.event` records on one line each. No raw JSON
  dumps in default output.
- A `CCB_RUN_REAL_CLAUDE=1`-gated integration test against real `claude`
  drives a deterministic prompt and confirms (a) at least one `tool.event`
  per minimum-set event, (b) Pre/Post pairing by `tool_use_id` is ordered,
  (c) `Stop` precedes `agent.done`.
- Relay bin's wall-clock budget (500 ms) is enforced by test and observable
  via the stderr `ccb-hook-relay:` prefix on every failure path.
- Negative criterion: no M3 PR introduces a `stream-json` reader, a PTY-output
  parser for semantic content, or any `permissionDecision`-writing path.

## Deferred to phase 2 of M3

In-scope for M3 conceptually, but explicitly deferred from phase 1. Each gets
a one-line rationale.

- `SessionStart` — partially redundant with the bridge's existing
  `session.started`.
- `SessionEnd` — partially redundant with the bridge's existing
  `session.ended`.
- `UserPromptSubmit` — partially redundant with the channel `prompt` the
  bridge already records on the inbound path.
- `PreCompact` / `PostCompact` — low value per event for a tool-timeline UI;
  carry no tool data.
- `PostToolUseFailure` — useful but partially carried by `PostToolUse`
  payloads when claude's hook surface exposes failure as part of the same
  event; defer until phase 2 confirms the on-the-wire shape.
- `Notification` — broad catch-all; surfaces auth dialogs and unrelated
  events; low signal-to-noise for tool timeline.
- `SubagentStart` / `SubagentStop` — subagents' own tool calls surface via
  their own `PreToolUse` / `PostToolUse` already; the start/stop markers add
  value but are not part of the minimum tool timeline.

## Out of scope

- Permission-prompt routing. Deferred to M4 (see `M4.md`).
- Hook-driven behavior modification (block tool calls; modify prompts;
  write `permissionDecision`).
- A separate websocket adapter for hook events. The HTTP / WS adapter is its
  own future milestone; M3 ships hooks over the existing transport only.
- Multi-session hook routing in a daemon. Per M1, the bridge is library +
  per-invocation CLI; no daemon.
- Replay-from-cursor for `events(sessionId, {since})`. The JSONL store
  records `tool.event` rows the same way it records every other event;
  cross-process replay remains deferred per M2's Out of Scope.

## Open questions

These are genuinely unresolved AFTER the locked decisions above. None of them
block starting M3.1.

- **Auto-merge vs documented-only `hooks-config`.** Phase 1 emits the snippet
  to stdout / `--out`. Should `ccb hooks-config --merge ~/.claude/settings.json`
  exist as an explicit opt-in for users with no pre-existing hooks? Auto-merge
  risks breaking unrelated hook entries; documented-only adds friction. Held
  open because the answer depends on how often consumers want this outside the
  managed-launch path, which already sidesteps the question entirely.
- **Hook relay reconnect behavior on transient bridge unavailability.** The
  bin opens a fresh TCP connection per fire and exits 0 on failure. A flaky
  bridge (e.g. mid-restart) loses observability silently. No retry in phase 1
  is the YAGNI move; revisit if real-claude smokes show meaningful loss.
- **Redaction of sensitive hook payloads.** `tool_input.command` for Bash
  often contains tokens / env-like content; `tool_result` for `Read` may
  contain secrets. Phase 1 persists raw payloads (truncated only). Held open:
  consumers asking for redaction can opt-in via a future
  `Bridge.hookRedactor?: (payload) => payload` callback. Not in M3 scope.

## Background reading

- `README.md` — entry point; M3 updates the Status section and cross-links
  from the CLI command list.
- `docs/ARCHITECTURE.md` — names hooks as the natural extension point for the
  gap M3 closes.
- `docs/M2.md` — managed launch, supervisor-crash event emission, and the
  `supervisor.start` / `ControlServer.deliver` hello-gates (shipped during
  M2; M3.4 below is already satisfied). This is the scope this milestone stacks
  on.
- [Claude Code hooks reference](https://code.claude.com/docs/en/hooks) — event
  list, payload shapes, settings.json schema.
